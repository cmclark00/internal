# wpa_supplicant minimal configuration for muOS with rtl8188eu support
# This is a simplified config that requires minimal dependencies

# CRITICAL: Driver support for rtl8188eu
CONFIG_DRIVER_NL80211=y
CONFIG_DRIVER_WEXT=y

# Use OpenSSL instead of internal TLS (easier to compile)
CONFIG_TLS=openssl

# Basic libnl support (you may need to install libnl-3-dev or disable this)
# If compilation fails, comment out the next line
CONFIG_LIBNL32=y

# Core IEEE 802.11 support
CONFIG_IEEE80211N=y
CONFIG_IEEE80211W=y

# WPA/WPA2 support (WPA3 disabled to reduce dependencies)
CONFIG_WPA3_SAE=y

# Basic EAP methods (minimal set)
CONFIG_EAP=y
CONFIG_EAP_TLS=y
CONFIG_EAP_PEAP=y
CONFIG_EAP_TTLS=y
CONFIG_EAP_MD5=y
CONFIG_EAP_MSCHAPV2=y

# PKCS#12 support
CONFIG_PKCS12=y

# Control interface (required for wpa_cli)
CONFIG_CTRL_IFACE=y
CONFIG_CTRL_IFACE_UNIX=y

# Backend for configuration files
CONFIG_BACKEND=file

# Wi-Fi Protected Setup (WPS)
CONFIG_WPS=y

# IEEE 802.11r (Fast BSS Transition) - optional
CONFIG_IEEE80211R=y

# Debug support (can be disabled for smaller binary)
CONFIG_DEBUG_FILE=y
CONFIG_DEBUG_SYSLOG=y

# Privilege separation
CONFIG_PRIVSEP=y
